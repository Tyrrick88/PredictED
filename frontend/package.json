{
  "scripts": {
    "build": "node scripts/build.js && npm run build:css",
    "build:css": "tailwindcss -c tailwind.config.js -i ./styles.css -o ./dist/assets/app.css --minify"
  },
  "devDependencies": {
    "@tailwindcss/container-queries": "^0.1.1",
    "@tailwindcss/forms": "^0.5.10",
    "tailwindcss": "^3.4.17"
  }
}
