[variables]
NIXPACKS_JDK_VERSION = "21"

[phases.setup]
nixPkgs = ["jdk21", "maven"]

[phases.build]
cmds = ["cd backend && mvn -DskipTests package"]

[start]
cmd = "java -jar backend/target/predicted-api-0.1.0-SNAPSHOT.jar"
